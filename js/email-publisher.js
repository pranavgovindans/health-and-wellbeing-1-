document.addEventListener('DOMContentLoaded',()=>{const form=document.getElementById('newsForm');if(!form)return;form.onsubmit=async event=>{event.preventDefault();const title=document.getElementById('newsTitle').value.trim(),copy=document.getElementById('newsCopy').value.trim(),button=form.querySelector('button');if(!title||!copy)return;button.disabled=true;button.textContent='Sending email...';try{const response=await fetch('/api/send-email',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({title,copy})});if(!response.ok)throw new Error('Email failed');alert('Published and emailed to pranavgovindans@gmail.com.');form.reset();}catch(_){alert('Email could not be sent. Check the Vercel Gmail environment variables.');}finally{button.disabled=false;button.textContent='Publish to Health News';}};});
