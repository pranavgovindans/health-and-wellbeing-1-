const $=id=>document.getElementById(id);let running=false,secs=0,timer,points=[];const canvas=$('heartChart'),ctx=canvas.getContext('2d');function draw(){const ratio=devicePixelRatio||1,w=canvas.clientWidth,h=210;canvas.width=w*ratio;canvas.height=h*ratio;ctx.scale(ratio,ratio);ctx.clearRect(0,0,w,h);ctx.strokeStyle='#edf0ee';ctx.lineWidth=1;for(let y=30;y<h;y+=45){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(w,y);ctx.stroke()}ctx.strokeStyle='#e7665c';ctx.lineWidth=2.5;ctx.beginPath();const arr=points.length?points:Array.from({length:90},(_,i)=>i);arr.forEach((v,i)=>{let x=i*w/(arr.length-1),base=h/2,wav=running?(Math.sin(i*.9)*7+(i%10===5?-50:0)+(i%10===6?25:0)):Math.sin(i*.2)*2,y=base+wav;i?ctx.lineTo(x,y):ctx.moveTo(x,y)});ctx.stroke()}function update(){const rate=68+Math.floor(Math.random()*11);$('heartRate').textContent=rate;$('respiration').innerHTML=`${14+Math.floor(Math.random()*4)} <em>br/min</em>`;$('temperature').innerHTML=`${(36.5+Math.random()*.5).toFixed(1)} <em>°C</em>`;$('oxygen').innerHTML=`${97+Math.floor(Math.random()*3)} <em>%</em>`;secs++;$('chartTime').textContent=`${String(Math.floor(secs/60)).padStart(2,'0')}:${String(secs%60).padStart(2,'0')}`;points.push(rate);if(points.length>100)points.shift();draw()}$('heartToggle').onclick=()=>{running=!running;if(running){$('heartToggle').textContent='Stop monitoring';$('heartMessage').textContent='Monitoring your vitals live';$('heartStatus').className='live-status active';$('heartStatus').innerHTML='<i></i> MONITORING LIVE';timer=setInterval(update,1000);update()}else{clearInterval(timer);$('heartToggle').textContent='Start monitoring';$('heartMessage').textContent='Monitoring paused';$('heartStatus').className='live-status';$('heartStatus').innerHTML='<i></i> READY TO MONITOR'}};addEventListener('resize',draw);draw();
